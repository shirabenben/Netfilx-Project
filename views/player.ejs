<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= episode.title %> - Player</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="player-page">
<!-- Include Navigation Bar -->
<%- include('partials/_Navbar') %>

<div class="player-container">
  <video id="video-player" src="<%= episode.videoUrl %>" controls></video>

  <div class="controls">
    <button id="rewind" class="btn-control">⏪ 10s</button>
    <button id="play-pause" class="btn-control">▶️ / ⏸</button>
    <button id="forward" class="btn-control">⏩ 10s</button>
    <input type="range" id="timeline" class="form-range timeline" min="0" value="0">
    <button id="fullscreen" class="btn-control">⛶</button>
    <button id="nextEpisode" class="btn-control">Next ▶️</button>
    <button id="toggleEpisodes" class="btn-control">📺 Episodes</button>
  </div>
</div>

<div id="episodesOverlay" class="overlay-episodes">
  <button id="closeOverlay" class="btn btn-sm">✖ Close</button>
  <h3>Episodes</h3>
  <% if (episodes.length > 0) { %>
    <% episodes.forEach(ep => { %>
      <div class="episode-item <%= ep._id.toString() === episode._id.toString() ? 'current-episode' : '' %>" 
           onclick="window.location.href='/player/<%= ep._id %>'">
        <img src="<%= ep.imageUrl || '/placeholder.jpg' %>" alt="Thumb" class="episode-thumb">
        <div class="episode-title">
          <strong>Episode <%= ep.episodeNumber %></strong> - <%= ep.title %>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <p>No episode list available.</p>
  <% } %>
</div>

<!-- Pass profile and content IDs to JS -->
<script>
  // ⚡ Ensure profile always exists to avoid ReferenceError
  window.PROFILE_ID = "<%= profile ? profile._id : '' %>";
  window.CONTENT_ID = "<%= episode._id %>";
  window.EPISODES = <%- JSON.stringify(episodes) %>;

  /**
   * Notes:
   * - PROFILE_ID is used to save watch progress and likes
   * - CONTENT_ID is the current video
   * - EPISODES contains all episodes for navigation
   */
</script>

<script src="/player.js"></script>

</body>
</html>
