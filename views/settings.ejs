<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Netflix Project - Manage Profiles</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/styles.css">
    
    <style>
        .settings-container {
            min-height: 100vh;
            padding: 80px 20px 40px;
        }

        .settings-header {
            text-align: center;
            margin-bottom: 50px;
        }

        .settings-title {
            color: white;
            font-size: 2.5rem;
            font-weight: 300;
            margin-bottom: 10px;
        }

        .settings-subtitle {
            color: #999;
            font-size: 1.1rem;
        }

        .profiles-management-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 0 auto 40px;
        }

        .profile-card {
            background: rgba(51, 51, 51, 0.8);
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            position: relative;
        }

        .profile-card:hover {
            transform: translateY(-5px);
            border-color: #e50914;
            box-shadow: 0 8px 20px rgba(229, 9, 20, 0.3);
        }

        .profile-card-avatar {
            width: 120px;
            height: 120px;
            margin: 0 auto 15px;
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            border: 3px solid #666;
        }

        .profile-card:hover .profile-card-avatar {
            border-color: #e50914;
        }

        .profile-card-avatar i {
            font-size: 60px;
            color: #999;
        }

        .profile-card-name {
            color: white;
            font-size: 1.2rem;
            font-weight: 500;
            margin-bottom: 15px;
            word-break: break-word;
        }

        .profile-card-actions {
            display: flex;
            gap: 10px;
            justify-content: center;
        }

        .profile-card-actions .btn {
            flex: 1;
            font-size: 0.85rem;
            padding: 8px 12px;
        }

        .add-profile-card {
            background: rgba(51, 51, 51, 0.4);
            border: 2px dashed #666;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            min-height: 240px;
        }

        .add-profile-card:hover {
            border-color: #e50914;
            background: rgba(51, 51, 51, 0.6);
        }

        .add-profile-card i {
            font-size: 50px;
            color: #999;
            margin-bottom: 15px;
        }

        .add-profile-card:hover i {
            color: #e50914;
        }

        .add-profile-text {
            color: #999;
            font-size: 1.1rem;
        }

        .add-profile-card:hover .add-profile-text {
            color: white;
        }

        .profile-limit-warning {
            text-align: center;
            color: #999;
            font-size: 0.9rem;
            margin-top: 20px;
        }

        .back-button-container {
            text-align: center;
            margin-top: 40px;
        }

        .modal-content {
            background: rgba(20, 20, 20, 0.98);
            border: 1px solid #333;
            color: white;
        }

        .modal-header {
            border-bottom: 1px solid #333;
        }

        .modal-footer {
            border-top: 1px solid #333;
        }

        .btn-close {
            filter: invert(1);
        }

        .alert {
            position: fixed;
            top: 80px;
            right: 20px;
            z-index: 9999;
            min-width: 300px;
            animation: slideIn 0.3s ease;
        }

        @keyframes slideIn {
            from {
                transform: translateX(400px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .profile-count {
            text-align: center;
            color: #999;
            font-size: 0.95rem;
            margin-bottom: 30px;
        }

        .profile-count strong {
            color: #e50914;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand text-danger fw-bold" href="/homepage<%= profile ? '?profile=' + profile._id : '' %>">
                <i class="fas fa-film me-2"></i>NETFLIX PROJECT
            </a>
            <div class="ms-auto">
                <a href="/profiles" class="btn btn-outline-light btn-sm">
                    <i class="fas fa-arrow-left me-2"></i>Back to Profiles
                </a>
            </div>
        </div>
    </nav>

    <div class="settings-container">
        <!-- Header -->
        <div class="settings-header">
            <h1 class="settings-title">Manage Profiles</h1>
            <div class="profile-count">
                You have <strong><span id="profile-count"><%= profiles ? profiles.length : 0 %></span></strong> of <strong>5</strong> profiles
            </div>
        </div>

        <!-- Profiles Grid -->
        <div class="profiles-management-grid" id="profiles-grid">
            <% if (profiles && profiles.length > 0) { %>
                <% profiles.forEach(profile => { %>
                    <div class="profile-card" data-profile-id="<%= profile._id %>">
                        <div class="profile-card-avatar">
                            <i class="fas fa-user-circle"></i>
                        </div>
                        <h3 class="profile-card-name"><%= profile.name %></h3>
                        <div class="profile-card-actions">
                            <button class="btn btn-sm btn-outline-light edit-profile-btn" 
                                    data-profile-id="<%= profile._id %>"
                                    data-profile-name="<%= profile.name %>">
                                <i class="fas fa-edit"></i> Edit
                            </button>
                            <button class="btn btn-sm btn-outline-danger delete-profile-btn" 
                                    data-profile-id="<%= profile._id %>"
                                    data-profile-name="<%= profile.name %>"
                                    <%= profiles.length <= 1 ? 'disabled' : '' %>>
                                <i class="fas fa-trash"></i> Delete
                            </button>
                        </div>
                    </div>
                <% }) %>
            <% } %>
            
            <!-- Add New Profile Card -->
            <div class="profile-card add-profile-card" id="add-profile-card" 
                 <%= (profiles && profiles.length >= 5) ? 'style="display:none;"' : '' %>>
                <i class="fas fa-plus-circle"></i>
                <p class="add-profile-text">Add Profile</p>
            </div>
        </div>
    </div>

    <!-- Create/Edit Profile Modal -->
    <div class="modal fade" id="profileModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="profileModalTitle">Add Profile</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="profileForm">
                        <input type="hidden" id="profileId" name="profileId">
                        <div class="mb-3">
                            <label for="profileName" class="form-label">Profile Name</label>
                            <input type="text" class="form-control" id="profileName" name="profileName" 
                                   placeholder="Enter profile name" required maxlength="50">
                            <div class="invalid-feedback" id="nameError"></div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-danger" id="saveProfileBtn">Save Profile</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Delete Confirmation Modal -->
    <div class="modal fade" id="deleteModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Delete Profile</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to delete the profile "<strong id="deleteProfileName"></strong>"?</p>
                    <p class="text-muted mb-0">This action cannot be undone.</p>
                    <input type="hidden" id="deleteProfileId">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-danger" id="confirmDeleteBtn">Delete Profile</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Settings JavaScript -->
    <script src="/settings.js"></script>
</body>
</html>

