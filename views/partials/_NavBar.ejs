<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
    <div class="container-fluid">
        <!-- Netflix Brand -->
        <a class="navbar-brand fw-bold fs-4 text-danger" href="/homepage">Netflix</a>
        
        <!-- Mobile Menu Toggle Button -->
        <button class="navbar-toggler border-danger" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu" 
                aria-controls="navMenu" aria-expanded="false" aria-label="Toggle navigation menu">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <!-- Main Navigation Menu -->
        <div class="navbar-collapse collapse" id="navMenu">
            <!-- Centered Search Bar -->
            <form class="d-flex mx-auto" role="search" action="/content/search" method="GET">
                <div class="input-group">
                    <input type="text" class="form-control" name="q" placeholder="Search movies and series..." 
                           value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>" style="min-width: 250px;">
                    <button class="btn btn-outline-danger" type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </form>
            
            <!-- All Navigation Items on Right -->
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link text-white fw-normal px-3 rounded" href="/movies">Movies</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fw-normal px-3 rounded" href="/series">Series</a>
                </li>
                <% if (typeof user !== 'undefined' && user && user.isAdmin) { %>
                <li class="nav-item">
                    <a class="nav-link text-warning fw-bold px-3 rounded" href="/add-content">
                        <i class="fas fa-plus-circle me-1"></i>Add Content
                    </a>
                </li>
                <% } %>
                <li class="nav-item">
                    <a class="nav-link text-white fw-normal px-3 rounded" href="/profiles">Profiles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-danger fw-normal px-3 rounded" href="/logout">
                        <i class="fas fa-sign-out-alt me-1"></i>Sign Out
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
