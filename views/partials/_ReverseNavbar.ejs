<!-- Reverse Navigation (Simple navbar with back button) -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand text-danger fw-bold" href="/homepage<%= typeof profile !== 'undefined' && profile ? '?profile=' + profile._id : '' %>">
            <i class="fas fa-film me-2"></i>NETFLIX PROJECT
        </a>
        <div class="ms-auto">
            <a href="/profiles" class="btn btn-outline-light btn-sm">
                <i class="fas fa-arrow-left me-2"></i>Back to Profiles
            </a>
        </div>
    </div>
</nav>